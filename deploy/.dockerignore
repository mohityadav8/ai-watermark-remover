# All images build with `service/` as the build context (see compose.yaml and
# service/Dockerfile*). Nothing in the repo root is COPY'd into those images.
# deploy/Dockerfile.web builds from the repo root and needs web/ + deploy/.
*
!web
!web/**
!deploy
!deploy/**
